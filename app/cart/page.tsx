// import CartSidebar from '@/components/cart-sidebar'
import React from 'react'

const page = () => {
  return (
    // <CartSidebar isOpen={true} closeCart={() => (false)} cartItems={cartItems} />
    <div></div>
  )
}

export default page
